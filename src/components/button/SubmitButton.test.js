describe('Submit button renders properly on initial render in different screens', () => {
    test.todo('button to be disabled by default')
    test.todo('button is enabled if there is event due to param in route')
  })

  describe('Submit button in all form screens should be', ()=> {
    test.todo('enabled on successful validation of all inputs')
    test.todo('on submit, the spinner should be enabled for 200-2000ms')
    test.todo('disabled when spinner is loading')
    test.todo('on successful submission, green tick should be highlighted for 50ms')
  })
  

  