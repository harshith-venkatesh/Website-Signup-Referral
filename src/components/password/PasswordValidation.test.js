describe('Input with label Password should be ', () => {
    test.todo('input should be of type password')
    test.todo('form field should have eye icon')
    test.todo('Password should show on click of eye')
    test.todo('Password should be disabled if user click on eye icon again')
  })
  
  describe('Test for password validation division', () => {
      test.todo('should be enabled if password field is not empty')
      test.todo('should display message based strength of the password [Weak, medium, strong]')
  })
  
  describe('Input with label Confirm Password should be', () => {
    test.todo('input should be of type password')
  })

  describe('Test for Confirm Password validation division', () => {
    test.todo('enabled if both password and confirm password field is not empty')
    test.todo('display success message if password and confirm password matches')
    test.todo('display failure message if password and confirm password does not matches')
})

